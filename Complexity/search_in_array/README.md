Le code est seulement composé d'une seule boucle for parcourant le tableau à la recherche de l'index du nombre recherché.
La boucle ayant une compléxité algorithmique égale à O(n), la compléxité globale du code est donc de O(n).
Le code a donc une compléxité algorithmique linéaire.
